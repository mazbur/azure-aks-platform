apiVersion: v1
kind: Service
metadata:
  name: {{ include "appHelmChart.fullname" . }}
  labels:
    {{- include "appHelmChart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "appHelmChart.selectorLabels" . | nindent 4 }}
