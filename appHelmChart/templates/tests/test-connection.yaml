apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "appHelmChart.fullname" . }}-test-connection"
  labels:
    {{- include "appHelmChart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "appHelmChart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
